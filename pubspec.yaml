name: lambula_vpn
description: Lambula VPN - LuVita · Eng. Anthony
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # SSH real
  dartssh2: ^2.9.0

  # HTTP e rede
  http: ^1.2.0

  # Armazenamento local
  shared_preferences: ^2.2.3


  # URL launcher (Facebook, etc)
  url_launcher: ^6.2.6


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true